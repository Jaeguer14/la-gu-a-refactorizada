@model AppMovie.Models.Return

@{
    Layout = "_Layout";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Detalles</title>
</head>
<body>

<div>
    <h4>Devolucion</h4>
    <hr />
    <dl class="row colorf fondotabla">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ReturnDate)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ReturnDate)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Partner)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Partner.PartnerName)
        </dd>
    </dl>
</div>

 <table class="table table-bordered text-center fondotabla">
        <thead>
            <tr class="">
                <th class="detalle1">Titulo De Pelicula</th>
                <th>
                    <div class="pokecoder"></div>
                </th>
            </tr>
        </thead>
        <tbody id="MovieReturn" >
           
        </tbody>
    </table>

<div>
    @* <a asp-action="Edit" asp-route-id="@Model?.ReturnID">Edit</a> | *@

    <a asp-action="Index" class="btn botonv">Volver</a>
</div>

@section Scripts{
    <script>
        window.onload = SearchMovieReturn(@Model?.ReturnID);
    </script>
}

</body>
</html>
